Dim aName as String
Dim anAge as String
Dim aHeight as String
Dim aWeight as String
Dim aJob as String
aName = "Bob"
anAge = "42"
aHeight = "72"
aWeight = "200"
aJob = "Realestate Agent"
Print aName + " works as a " + aJob + "."
Print aName + " is " + anAge + " years old."
Print aName + " is " + aHeight + " inches tall."
Print aName + " weighs " + aWeight + " pounds."
